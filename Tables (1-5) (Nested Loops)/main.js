// Tables 1 to 5
for (let tableNumber = 1; tableNumber < 6; tableNumber++) {
  for (let multiplicant = 1; multiplicant < 11; multiplicant++) {
    console.log(
      `${tableNumber} X ${multiplicant} = ${tableNumber * multiplicant}`
    );
  }
  console.log("");
}
